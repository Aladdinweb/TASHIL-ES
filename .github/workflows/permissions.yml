# COPYRIGHT ILINE TECH 2026 BY FERAK ALADDIN
name: Permissions check

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
    steps:
      - name: Check permissions
        run: echo "Permissions OK"
